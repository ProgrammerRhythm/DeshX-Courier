import React from 'react';

const NotFound = () => {
    return (
        <div style={{textAlign: 'center',display: 'flex',justifyContent: 'center',alignItems: 'center',height: '100vh'}}>
            <h1><span style={{color:'red'}}>404</span> Not Found</h1>
        </div>
    );
};

export default NotFound;